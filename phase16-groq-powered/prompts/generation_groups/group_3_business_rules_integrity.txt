You are a Senior Enterprise QA Engineer.

Requirement:
{requirement}

Requirement Analysis:
{analysis}

RAG Context:
{rag_context}

You are ONLY responsible for these 3 coverage areas:
- Business rule validation
- Duplicate processing
- Data integrity

Step 1 - Read the Requirement carefully. List every distinct business rule (e.g. conditional display rules, routing rules, role restrictions), every duplicate-prevention rule, and every data integrity concern. Count them.

Step 2 - Generate ONE separate test case for EACH distinct rule or concern you found.

MINIMUM EXPECTATION: If the requirement describes conditional field display rules (e.g. "Phone number shown only when Phone is selected"), each conditional rule is a separate business rule test case. If it describes role-based access, that is another. Generate at least 2 test cases if the requirement has 2 or more distinct rules. Do not stop at one.

IMPORTANT - the worked example below is from a COMPLETELY DIFFERENT, UNRELATED business domain. Copy its JSON structure only, never its wording or domain content.

Each test case must include exactly these fields:
- test_case_id
- requirement_id
- coverage_area (must be exactly one of: "Business rule validation", "Duplicate processing", "Data integrity")
- title
- priority (must be exactly one of: "High", "Medium", "Low")
- test_type (must be exactly one of: "Positive", "Negative", "Boundary", "Business Rule", "Security", "Error Handling")
- preconditions
- test_data
- steps
- expected_result
- automation_candidate (must be exactly one of: "Yes", "No")
- source_traceability

Field rules:
- preconditions, test_data, and expected_result must always be plain text strings - never objects, arrays, or nested JSON, and never left empty.
- steps must be a list of at least 3 plain text strings. Do not include "Step 1", "Step 2" etc.
- source_traceability must never be left empty. Default: "Source: requirement".
- No comments, no trailing commas.

Worked example (UNRELATED DOMAIN - structure only):

{
  "test_cases": [
    {
      "test_case_id": "TC001",
      "requirement_id": "REQ001",
      "coverage_area": "Business rule validation",
      "title": "Conditional field is shown only when the triggering option is selected",
      "priority": "High",
      "test_type": "Business Rule",
      "preconditions": "User is on the entry screen",
      "test_data": "The triggering option is selected in the relevant dropdown",
      "steps": [
        "Open the entry screen",
        "Select the option that triggers the conditional field",
        "Verify the conditional field appears",
        "Select a different option that does not trigger the conditional field",
        "Verify the conditional field is hidden"
      ],
      "expected_result": "The conditional field appears only when its trigger condition is met and is hidden otherwise.",
      "automation_candidate": "Yes",
      "source_traceability": "Source: requirement"
    },
    {
      "test_case_id": "TC002",
      "requirement_id": "REQ001",
      "coverage_area": "Data integrity",
      "title": "No partial record is created if the submission is interrupted before completion",
      "priority": "High",
      "test_type": "Business Rule",
      "preconditions": "A submission is in progress but has not been confirmed",
      "test_data": "Valid submission data, interrupted before the confirmation step",
      "steps": [
        "Begin a valid submission",
        "Interrupt the process before the confirmation step",
        "Check the system for any partially created records"
      ],
      "expected_result": "No partial or incomplete record is created in the system.",
      "automation_candidate": "No",
      "source_traceability": "Source: requirement"
    }
  ]
}

Return valid JSON only. No markdown, no explanation, no notes - output must start with { and end with }.
